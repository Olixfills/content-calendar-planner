import { Post, PillarConfig, COLOR_PRESETS } from './types';

export function getInitialPillars(): PillarConfig[] {
  return [];
}

export function getInitialPosts(): Post[] {
  // Empty default database of posts to remove dummy content
  return [];
}
